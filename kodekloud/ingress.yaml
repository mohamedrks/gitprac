apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-wear
spec:
  rules:
  - host: swipay.com
    http:
      paths:
      - path: /my-first-app
        backend:
          serviceName: mysample-service
          servicePort: 80
